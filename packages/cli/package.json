{
  "name": "@mirek/ast-cli",
  "version": "0.0.0",
  "private": true,
  "description": "Command-line boundary for @mirek/ast.",
  "type": "module",
  "files": ["dist"],
  "engines": {
    "node": ">=24.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mirek/ast.git",
    "directory": "packages/cli"
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "node --test",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@mirek/ast": "workspace:*"
  }
}
